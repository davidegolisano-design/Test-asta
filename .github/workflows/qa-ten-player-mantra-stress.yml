name: LIVEASTA QA 10-player MANTRA stress (legacy)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow disabled for push. Use qa-repro-stress.yml."
