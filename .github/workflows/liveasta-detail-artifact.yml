name: LIVEASTA detailed visual artifact
on:
  push:
    paths:
      - '.github/workflows/liveasta-detail-artifact.yml'
permissions:
  contents: read
jobs:
  artifact:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: liveasta-visual-detail
          path: dev/visual-audit/detail-contact-sheet.jpg
          if-no-files-found: error
